---
import * as messages from "../../paraglide/messages.js";
import { assertIsLocale } from "../../paraglide/runtime.js";
import ContactDetail from "../../components/contact/ContactDetail.astro";
import PublicLayout from "../../layouts/PublicLayout.astro";
import { getPublicationCatalog } from "../../lib/content/repository";
import {
  getFixedPagePath,
  getLocalizedFixedPageAlternatives,
} from "../../lib/content/routes";

export function getStaticPaths() {
  return [{ params: { locale: "en" } }];
}

const locale = assertIsLocale(Astro.currentLocale);
const catalog = await getPublicationCatalog();
const canonical = new URL(
  getFixedPagePath("contact", locale),
  Astro.site,
).toString();
---

<PublicLayout
  canonical={canonical}
  description={messages.contact_page_description({}, { locale })}
  locale={locale}
  localizedAlternatives={getLocalizedFixedPageAlternatives(
    "contact",
    Astro.site!,
  )}
  title={`${messages.contact_page_title({}, { locale })} | ${messages.site_title({}, { locale })}`}
>
  <ContactDetail catalog={catalog} locale={locale} />
</PublicLayout>
