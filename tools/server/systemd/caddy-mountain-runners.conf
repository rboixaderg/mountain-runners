[Service]
# The packaged Caddy unit uses ProtectSystem=strict and only writes to
# /var/log/caddy by default. T5.1 stores access/error logs under
# /var/log/mountain-runners and Caddy reads the active release from
# /var/lib/mountain-runners.
ReadWritePaths=/var/log/mountain-runners /var/lib/mountain-runners
